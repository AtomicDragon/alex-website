# Public Sanity config (safe to commit — these ship in the client bundle).
# Secrets (write tokens, auth) must go in .env.local, which is git-ignored.
VITE_SANITY_PROJECT_ID=62r37bvw
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-10-01
