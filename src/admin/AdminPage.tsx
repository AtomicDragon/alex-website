import Placeholder from '../components/layout/Placeholder';

export default function AdminPage() {
  return (
    <Placeholder
      title="Admin"
      subtitle="Authenticated dashboard, content manager, and private business tools (CRM, opportunities, notes, tasks)."
    />
  );
}
